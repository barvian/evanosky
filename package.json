{
  "name": "evanosky",
  "version": "0.0.0",
  "description": "Website for the Evanosky Foundation.",
  "private": "true",
  "scripts": {
    "deploy": "gulp deploy",
    "gulp": "gulp",
    "build": "gulp build",
    "watch": "gulp"
  },
  "devDependencies": {
    "gulpfile": "github:mbarvian/gulpfile.js"
  }
}
